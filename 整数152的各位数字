/*输出整数152的个位数字、十位数字和百位数字的值。

输入格式：
本题无输入。

输出格式：
按照以下格式输出：

152 = 个位数字 + 十位数字*10 + 百位数字*100*/

#include<stdio.h>
int main()
{
	int x = 152, a[3], i = 0;
	while (x > 0) {
		a[i++] = x % 10;
		x /= 10;
	}
	printf("152 = %d + %d*10 + %d*100", a[0], a[1], a[2]);
	return 0;
}
